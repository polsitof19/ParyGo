// Theming por marca para el sitio público del comprador.
// Las funciones de color viven en `@/lib/brandColors` (compartidas con los
// paneles que editan branding); acá se re-exportan para no romper imports.
export { brandColor, brandFillPair, brandFillHover, brandInk, brandMark, withAlpha } from '@/lib/brandColors';
